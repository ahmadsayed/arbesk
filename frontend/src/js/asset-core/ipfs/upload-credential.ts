/** Placeholder — Task 3 replaces this with the real moved module. */
export interface UploadCredential { backend: string; url?: string; urls?: string[]; apiUrl?: string; gateway?: string; reusable?: boolean }
