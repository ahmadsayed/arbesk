{
  "name": "@arbesk/ai-asset-gen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Arbesk 3D-model generation SDK: a capability-gated facade over the mock and Tripo3D providers (text/image/multiview-to-3D, retexture, retopo, rig, animate). Backend-only.",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./*": "./dist/*"
  },
  "files": ["dist"],
  "scripts": {
    "build": "npm run clean && tsc -p tsconfig.build.json",
    "clean": "node -e \"fs.rmSync('dist', { recursive: true, force: true })\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "license": "ISC"
}