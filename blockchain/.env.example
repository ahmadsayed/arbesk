# RPC endpoints — configure for your target network
# Hardhat local (Docker container):
# API_URL=http://127.0.0.1:8545
# MegaETH Testnet:
API_URL=https://carrot.megaeth.com/rpc

PRIVATE_KEY=
PUBLIC_KEY=

# ArbeskAssetFree (free tier) — default contract
CONTRACT_ADDRESS=

# ArbeskAsset (paid tier) — local dev only, not deployed on testnet
PAID_CONTRACT_ADDRESS=

# USDC token address — local dev only, not deployed on testnet
USDC_TOKEN=

TREASURY_ADDRESS=

# Etherscan API key — used for contract verification
ETHERSCAN_API_KEY=

ASSETS_IPFS=
